export enum Visibility {
    Public = 'public',
    Private = 'private',
}

export enum Status {
    Active = 'active',
    Inactive = 'inactive',
}

export enum Interaction {
    ProductCard = 'product-card',
    HomePage = 'home-page',
}
